## 输出占位符
```
  %d int
  %f float
  %ld long
  %c char
  %p
```

## 字符型
```
  字符常量
    只能单引号，只能是单个字符，可以是任意字符
    'a', 'b', '=', '+', '?'


  字符变量
    用来储存字符常量，即单个字符
    char a,b;
  
  转义字符
    特殊的字符常量，以反斜杠开头，有特殊的意义
    \n 回车
  
  字符在内存存储形式
    每个字符对应一个 ASCII 码
    一个字符占一个字节空间，内存中以 ASCII 的形式存储。
```